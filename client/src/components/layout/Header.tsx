/**
 * @deprecated This component has been removed. Please use MainHeader.tsx instead.
 * This file is now just a proxy that imports and exports MainHeader to avoid breaking existing code.
 */

import { MainHeader } from "./MainHeader";

// Re-export MainHeader as Header for backward compatibility
export { MainHeader as Header };